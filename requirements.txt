Django==3.0.14
# postgreSQLドライバ
psycopg2==2.7.6
psycopg2-binary==2.7.7
# herokuに必要
gunicorn
django-heroku
whitenoise
# DRFに必要
markdown
django-filter
djangorestframework==3.11.2
# React（localhost:3000）からDjango（localhost:8000）のAPIを叩く際に必要
# django-cors-headers==3.2.1
# シークレット環境変数の設定に必要
django-environ
# social loginに必要
social-auth-app-django
# recapcthaの認証機能をdjangoに全て任せる
django-recaptcha2
# モデルの全オブジェクトを1ユーザー毎に権限設定をするのに必要
django-guardian
# オブジェクトをさらに行レベルで権限設定するのに必要
rules